/**
 * @param {string} s
 * @return {string}
 */
var longestPalindrome = function (s) {
    let len = s.length // 字符串的长度
    let left = 0,
        right = 0 // 中心点两边的左右指针
    let maxLength = 0 // 回文子串的最大长度

    for (let i = 0; i < len; i++) {
        let subLeft = i,
            subRight = i;

    }
};